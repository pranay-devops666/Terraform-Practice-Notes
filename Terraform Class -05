Terraform Class -05

TOPIC: TERRAFORM REFRESH, PROVISIONERS
========================================



Lunch one instance --------------> name: terraform-----------> with keypair-------> securiy group--------> 10gb ebs volume ----------> launch and connect 

Now we need to install Terraform in it

go to this website -------> https://developer.hashicorp.com/terraform/install--------> select Linux ---------> select Amazon Linux------>copy all commands and paste here 

go to this website to install locally on laptop ----------------------> https://developer.hashicorp.com/terraform/tutorials/aws-get-started/install-cli

[root@TERRAFORM ~]# sudo yum install -y yum-utils shadow-utils 
sudo yum-config-manager --add-repo https://rpm.releases.hashicorp.com/AmazonLinux/hashicorp.repo
sudo yum install terraform

[root@TERRAFORM ~]# terraform version ---------------------->
Terraform v1.14.8
on linux_amd64
[root@TERRAFORM ~]# 
[root@TERRAFORM ~]# terraform version
Terraform v1.14.8
on linux_amd64
[root@TERRAFORM ~]# 

lets write the code here 



[root@TERRAFORM ~]# vim provider.tf
[root@TERRAFORM ~]# terraform fmt
provider.tf
[root@TERRAFORM ~]# cat provider.tf
provider "aws" {
  region = "us-east-1"
}
[root@TERRAFORM ~]#


[root@TERRAFORM ~]# vim main.tf
resource "aws_instance" "myinstance" {
tags = {
Name = "My-Server"
}
ami = "ami-0ea87431b78a82070"
instance_type = var.itype
}


variable "itype" {
type = string
}

:wq


[root@TERRAFORM ~]# terraform init
Initializing the backend...
Initializing provider plugins...
- Finding latest version of hashicorp/aws...
- Installing hashicorp/aws v6.40.0...
- Installed hashicorp/aws v6.40.0 (signed by HashiCorp)
Terraform has created a lock file .terraform.lock.hcl to record the provider
selections it made above. Include this file in your version control repository
so that Terraform can guarantee to make the same selections by default when
you run "terraform init" in the future.

Terraform has been successfully initialized!

You may now begin working with Terraform. Try running "terraform plan" to see
any changes that are required for your infrastructure. All Terraform commands
should now work.

If you ever set or change modules or backend configuration for Terraform,
rerun this command to reinitialize your working directory. If you forget, other
commands will detect it and remind you to do so if necessary.
[root@TERRAFORM ~]# terraform plan
var.itype
  Enter a value: t3.micro


[root@TERRAFORM ~]# terraform plan -var="itype=t3.micro" ------------------------------->


[root@TERRAFORM ~]# terraform plan -var="itype=t3.micro, iname=, vol=" ----------------------------->



TOPIC: TERRAFORM REFRESH
=============================

vim resource.tf

resource "aws_instance" "myinstance" {
tags = {
Name = "My-Server"
}
ami = "ami-0ea87431b78a82070"
instance_type = var.itype
}


variable "itype" {
type = string
default = "t3.micro"
}

:wq

[root@TERRAFORM ~]# vim main.tf ---------------------------------------------->
[root@TERRAFORM ~]# terraform init ---------------------------------------------->
Initializing the backend...
Initializing provider plugins...
- Reusing previous version of hashicorp/aws from the dependency lock file
- Using previously-installed hashicorp/aws v6.40.0

Terraform has been successfully initialized!

You may now begin working with Terraform. Try running "terraform plan" to see
any changes that are required for your infrastructure. All Terraform commands
should now work.

If you ever set or change modules or backend configuration for Terraform,
rerun this command to reinitialize your working directory. If you forget, other
commands will detect it and remind you to do so if necessary.
[root@TERRAFORM ~]# terraform plan ---------------------------------------------->

Terraform used the selected providers to generate the following execution plan. Resource actions are indicated with the following symbols:
  + create

Terraform will perform the following actions:

  # aws_instance.myinstance will be created
  + resource "aws_instance" "myinstance" {
      + ami                                  = "ami-0ea87431b78a82070"
      + arn                                  = (known after apply)
      + associate_public_ip_address          = (known after apply)
      + availability_zone                    = (known after apply)
      + disable_api_stop                     = (known after apply)
      + disable_api_termination              = (known after apply)
      + ebs_optimized                        = (known after apply)
      + enable_primary_ipv6                  = (known after apply)
      + force_destroy                        = false
      + get_password_data                    = false
      + host_id                              = (known after apply)
      + host_resource_group_arn              = (known after apply)
      + iam_instance_profile                 = (known after apply)
      + id                                   = (known after apply)
      + instance_initiated_shutdown_behavior = (known after apply)
      + instance_lifecycle                   = (known after apply)
      + instance_state                       = (known after apply)
      + instance_type                        = "t3.micro"
      + ipv6_address_count                   = (known after apply)
      + ipv6_addresses                       = (known after apply)
      + key_name                             = (known after apply)
      + monitoring                           = (known after apply)
      + outpost_arn                          = (known after apply)
      + password_data                        = (known after apply)
      + placement_group                      = (known after apply)
      + placement_group_id                   = (known after apply)
      + placement_partition_number           = (known after apply)
      + primary_network_interface_id         = (known after apply)
      + private_dns                          = (known after apply)
      + private_ip                           = (known after apply)
      + public_dns                           = (known after apply)
      + public_ip                            = (known after apply)
      + region                               = "us-east-1"
      + secondary_private_ips                = (known after apply)
      + security_groups                      = (known after apply)
      + source_dest_check                    = true
      + spot_instance_request_id             = (known after apply)
      + subnet_id                            = (known after apply)
      + tags                                 = {
          + "Name" = "My-Server"
        }
      + tags_all                             = {
          + "Name" = "My-Server"
        }
      + tenancy                              = (known after apply)
      + user_data_base64                     = (known after apply)
      + user_data_replace_on_change          = false
      + vpc_security_group_ids               = (known after apply)

      + capacity_reservation_specification (known after apply)

      + cpu_options (known after apply)

      + ebs_block_device (known after apply)

      + enclave_options (known after apply)

      + ephemeral_block_device (known after apply)

      + instance_market_options (known after apply)

      + maintenance_options (known after apply)

      + metadata_options (known after apply)

      + network_interface (known after apply)

      + primary_network_interface (known after apply)

      + private_dns_name_options (known after apply)

      + root_block_device (known after apply)

      + secondary_network_interface (known after apply)
    }

Plan: 1 to add, 0 to change, 0 to destroy.

──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────

Note: You didn't use the -out option to save this plan, so Terraform can't guarantee to take exactly these actions if you run "terraform apply" now.
[root@TERRAFORM ~]# terraform apply ---------------------------------------------->
Plan: 1 to add, 0 to change, 0 to destroy.

Do you want to perform these actions?
  Terraform will perform the actions described above.
  Only 'yes' will be accepted to approve.

  Enter a value: yes

aws_instance.myinstance: Creating...
aws_instance.myinstance: Still creating... [00m10s elapsed]
aws_instance.myinstance: Creation complete after 13s [id=i-048d94689fdcf11fc]

Apply complete! Resources: 1 added, 0 changed, 0 destroyed.
[root@TERRAFORM ~]# 

[root@TERRAFORM ~]# terraform plan -refresh-only ---------------------------------------------->

Note: Objects have changed outside of Terraform

Terraform detected the following changes made outside of Terraform since the last "terraform apply" which may have affected this plan:

  # aws_instance.myinstance has changed
  ~ resource "aws_instance" "myinstance" {
        id                                   = "i-048d94689fdcf11fc"
      ~ tags                                 = {
          ~ "Name" = "My-Server" -> "Pranay-Instance"
        }
      ~ tags_all                             = {
          ~ "Name" = "My-Server" -> "Pranay-Instance"
        }
        # (39 unchanged attributes hidden)

        # (9 unchanged blocks hidden)
    }


This is a refresh-only plan, so Terraform will not take any actions to undo these. If you were expecting these changes then you can apply this plan to record the
updated values in the Terraform state without changing any remote objects.

──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────

Note: You didn't use the -out option to save this plan, so Terraform can't guarantee to take exactly these actions if you run "terraform apply" now.
[root@TERRAFORM ~]# 

[root@TERRAFORM ~]# terraform apply ----------------------------------------->

Terraform used the selected providers to generate the following execution plan. Resource actions are indicated with the following symbols:
  ~ update in-place

Terraform will perform the following actions:

  # aws_instance.myinstance will be updated in-place
  ~ resource "aws_instance" "myinstance" {
        id                                   = "i-048d94689fdcf11fc"
      ~ tags                                 = {
          ~ "Name" = "Pranay-Instance" -> "My-Server"
        }
      ~ tags_all                             = {
          ~ "Name" = "Pranay-Instance" -> "My-Server"
        }
        # (39 unchanged attributes hidden)

        # (9 unchanged blocks hidden)
    }

Plan: 0 to add, 1 to change, 0 to destroy.

Do you want to perform these actions?
  Terraform will perform the actions described above.
  Only 'yes' will be accepted to approve.

  Enter a value: yes

aws_instance.myinstance: Modifying... [id=i-048d94689fdcf11fc]
aws_instance.myinstance: Modifications complete after 1s [id=i-048d94689fdcf11fc]

Apply complete! Resources: 0 added, 1 changed, 0 destroyed.
[root@TERRAFORM ~]#

[root@TERRAFORM ~]# cat main.tf 
resource "aws_instance" "myinstance" {
tags = {
Name = "My-Server"
}
ami = "ami-0ea87431b78a82070"
instance_type = var.itype
count = 2
}


variable "itype" {
type = string
default = "t3.micro"
}
[root@TERRAFORM ~]# 

[root@TERRAFORM ~]# terraform apply ----------------------------------->

Plan: 1 to add, 0 to change, 0 to destroy.

Do you want to perform these actions?
  Terraform will perform the actions described above.
  Only 'yes' will be accepted to approve.

  Enter a value: yes

aws_instance.myinstance[1]: Creating...
aws_instance.myinstance[1]: Still creating... [00m10s elapsed]
aws_instance.myinstance[1]: Creation complete after 13s [id=i-02a89781348985c74]

Apply complete! Resources: 1 added, 0 changed, 0 destroyed.
[root@TERRAFORM ~]# 



[root@TERRAFORM ~]# terraform state list ---------------------------------->

[root@TERRAFORM ~]# terraform state list ---------------------------------->
aws_instance.myinstance[0]
aws_instance.myinstance[1]
[root@TERRAFORM ~]# 


[root@TERRAFORM ~]# terraform apply -refresh-only ------------------------------>

[root@TERRAFORM ~]# terraform apply -refresh-only
aws_instance.myinstance[1]: Refreshing state... [id=i-02a89781348985c74]
aws_instance.myinstance[0]: Refreshing state... [id=i-048d94689fdcf11fc]

No changes. Your infrastructure still matches the configuration.

Terraform has checked that the real remote objects still match the result of your most recent changes, and found no differences.

Would you like to update the Terraform state to reflect these detected changes?
  Terraform will write these changes to the state without modifying any real infrastructure.
  There is no undo. Only 'yes' will be accepted to confirm.

  Enter a value: yes


Apply complete! Resources: 0 added, 0 changed, 0 destroyed.
[root@TERRAFORM ~]# 

[root@TERRAFORM ~]# cat main.tf 
resource "aws_instance" "myinstance" {
tags = {
Name = "My-Server"
}
ami = "ami-0ea87431b78a82070"
instance_type = var.itype
count = 1
}


variable "itype" {
type = string
default = "t3.micro"
}
[root@TERRAFORM ~]# 

[root@TERRAFORM ~]# terraform destroy --auto-approve ---------------------->

[root@TERRAFORM ~]# cat main.tf 
variable "itype" {
  type    = string
  default = "t3.micro"
}

resource "aws_instance" "myinstance" {
  count = 1

  tags = {
    Name = "My-Server"
  }

  ami           = "ami-0ea87431b78a82070"
  instance_type = var.itype

  provisioner "local-exec" {
    command = "echo 'Instance created with id: ${aws_instance.myinstance[count.index].id}' >> server-id.txt"
  }
}
[root@TERRAFORM ~]# 

[root@TERRAFORM ~]# terraform apply --auto-approve ---------------------------->


Plan: 1 to add, 0 to change, 0 to destroy.
aws_instance.myinstance[0]: Creating...
aws_instance.myinstance[0]: Still creating... [00m10s elapsed]
aws_instance.myinstance[0]: Provisioning with 'local-exec'...
aws_instance.myinstance[0] (local-exec): Executing: ["/bin/sh" "-c" "echo 'Instance created with id: i-08258883ebec958f8' >> server-id.txt"]
aws_instance.myinstance[0]: Creation complete after 13s [id=i-08258883ebec958f8]

Apply complete! Resources: 1 added, 0 changed, 0 destroyed.
[root@TERRAFORM ~]# 

[root@TERRAFORM ~]# ll
total 24
-rw-r--r--. 1 root root  370 Apr  9 12:23 main.tf
-rw-r--r--. 1 root root   42 Apr  9 11:46 provider.tf
-rw-r--r--. 1 root root   46 Apr  9 12:23 server-id.txt
-rw-r--r--. 1 root root 5431 Apr  9 12:23 terraform.tfstate
-rw-r--r--. 1 root root  181 Apr  9 12:23 terraform.tfstate.backup
[root@TERRAFORM ~]# 
[root@TERRAFORM ~]# cat server-id.txt 
Instance created with id: i-08258883ebec958f8
[root@TERRAFORM ~]# 


[root@TERRAFORM ~]# terraform destroy --auto-approve ---------------------------->


Plan: 0 to add, 0 to change, 1 to destroy.
aws_instance.myinstance[0]: Destroying... [id=i-08258883ebec958f8]
aws_instance.myinstance[0]: Still destroying... [id=i-08258883ebec958f8, 00m10s elapsed]
aws_instance.myinstance[0]: Still destroying... [id=i-08258883ebec958f8, 00m20s elapsed]
aws_instance.myinstance[0]: Destruction complete after 30s

Destroy complete! Resources: 1 destroyed.
[root@TERRAFORM ~]# 


[root@TERRAFORM ~]# ssh-keygen
Generating public/private rsa key pair.
Enter file in which to save the key (/root/.ssh/id_rsa): 
Enter passphrase (empty for no passphrase): 
Enter same passphrase again: 
Your identification has been saved in /root/.ssh/id_rsa
Your public key has been saved in /root/.ssh/id_rsa.pub
The key fingerprint is:
SHA256:JFRsnXPGn7JLb6dsEHTTvZ3H/O2DMxRG8CvW9+WsfFw root@TERRAFORM
The key's randomart image is:
+---[RSA 3072]----+
|      .o.. +.  ..|
|     .  o + *.o o|
|      ...  =.+ =+|
|       o    +o+o=|
|        S  o.*..=|
|          . =..oE|
|           ..+.o=|
|            .*+o=|
|             oB+.|
+----[SHA256]-----+
[root@TERRAFORM ~]# 

[root@TERRAFORM ~]# cd .ssh/
[root@TERRAFORM .ssh]# ll
total 12
-rw-------. 1 root root  563 Apr  9 11:42 authorized_keys
-rw-------. 1 root root 2602 Apr  9 12:27 id_rsa
-rw-r--r--. 1 root root  568 Apr  9 12:27 id_rsa.pub
[root@TERRAFORM .ssh]# cd 
[root@TERRAFORM ~]# vim main.tf 
[root@TERRAFORM ~]# cat main.tf 
resource "aws_key_pair" "mykey" {
  key_name = "pranay"
  public_key = file("~/.ssh/id_rsa.pub")
}


variable "itype" {
  type    = string
  default = "t3.micro"
}

resource "aws_instance" "myinstance" {
  count = 1

  tags = {
    Name = "My-Server"
  }

  ami           = "ami-0ea87431b78a82070"
  instance_type = var.itype

  provisioner "remote-exec" {
    connection {
      type = "ssh"
      user = "ec2-user"
      private_key = file("~/.ssh/id_rsa")
      host = self.public_ip
  }
  inline = [
    "sudo yum update -y"
    "sudo yum install httpd -y"
    "sudo systemctl start httpd"
}
[root@TERRAFORM ~]# 

[root@TERRAFORM ~]# cat main.tf 
resource "aws_key_pair" "mykey" {
  key_name = "pranay"
  public_key = file("~/.ssh/id_rsa.pub")
}


[root@TERRAFORM ~]# vim security.tf
[root@TERRAFORM ~]# cat security.tf
resource "aws_security_group" "mysg" {
name = "terraform-sg"
description = "its created by tf"


ingress {
from_port = 22
to_port = 22
protocol = "tcp"
cidr_blocks = ["0.0.0.0/0"]
}

ingress {
from_port = 80
to_port = 80
protocol = "tcp"
cidr_blocks = ["0.0.0.0/0"]
}

egress {
from_port = 0
to_port = 0
protocol = "-1"
cidr_blocks = ["0.0.0.0/0"]
}

}
[root@TERRAFORM ~]# 

[root@TERRAFORM ~]# cat main.tf 
resource "aws_key_pair" "mykey" {
  key_name = "pranay"
  public_key = file("~/.ssh/id_rsa.pub")
}


variable "itype" {
  type    = string
  default = "t3.micro"
}

resource "aws_instance" "myinstance" {
  count = 1

  tags = {
    Name = "My-Server"
  }

  ami           = "ami-0ea87431b78a82070"
  instance_type = var.itype
  key_name      = aws_key_pair.mykey.key_name
  vpc_security_group_ids = [aws_security_group.mysg.id]

  provisioner "remote-exec" {
    connection {
      type = "ssh"
      user = "ec2-user"
      private_key = file("~/.ssh/id_rsa")
      host = self.public_ip
  }
  inline = [
    "sudo yum update -y",
    "sudo yum install httpd -y",
    "sudo systemctl start httpd"
  ]
}
}

:wq


[root@TERRAFORM ~]# 

[root@TERRAFORM ~]# terraform apply --auto-approve -------------------------->

aws_instance.myinstance[0] (remote-exec): Complete!
aws_instance.myinstance[0]: Creation complete after 1m2s [id=i-0661b1916facfc0c6]

Apply complete! Resources: 1 added, 0 changed, 1 destroyed.
[root@TERRAFORM ~]# 

http://54.86.70.240/ -----------------> enter (this ip was created terraform node server)

output will come as 

it works

Topic: sending a file to remote server like below

[root@TERRAFORM ~]# ll
total 40
-rw-r--r--. 1 root root  706 Apr  9 12:56 main.tf
-rw-r--r--. 1 root root   42 Apr  9 11:46 provider.tf
-rw-r--r--. 1 root root  353 Apr  9 12:40 security.tf
-rw-r--r--. 1 root root   46 Apr  9 12:23 server-id.txt
-rw-r--r--. 1 root root 9433 Apr  9 12:58 terraform.tfstate
-rw-r--r--. 1 root root 9430 Apr  9 12:57 terraform.tfstate.backup
[root@TERRAFORM ~]# vim main.tf 
[root@TERRAFORM ~]# terraform fmt
main.tf
security.tf
[root@TERRAFORM ~]# 

[root@TERRAFORM ~]# cat main.tf 
resource "aws_key_pair" "mykey" {
  key_name   = "pranay"
  public_key = file("~/.ssh/id_rsa.pub")
}


variable "itype" {
  type    = string
  default = "t3.micro"
}

resource "aws_instance" "myinstance" {
  count = 1

  tags = {
    Name = "My-Server"
  }

  ami                    = "ami-0ea87431b78a82070"
  instance_type          = var.itype
  key_name               = aws_key_pair.mykey.key_name
  vpc_security_group_ids = [aws_security_group.mysg.id]

  provisioner "file" {
    connection {
      type        = "ssh"
      user        = "ec2-user"
      private_key = file("~/.ssh/id_rsa")
      host        = self.public_ip
    }
    source = "/root/server-id.txt"
    destination = "/home/ec2-user/"
  }
}
[root@TERRAFORM ~]# 


[root@TERRAFORM ~]# terraform apply --auto-approve
aws_key_pair.mykey: Refreshing state... [id=pranay]
aws_security_group.mysg: Refreshing state... [id=sg-06e5ea6615e0c83a4]
aws_instance.myinstance[0]: Refreshing state... [id=i-0661b1916facfc0c6]

No changes. Your infrastructure matches the configuration.

Terraform has compared your real infrastructure against your configuration and found no differences, so no changes are needed.

Apply complete! Resources: 0 added, 0 changed, 0 destroyed.
[root@TERRAFORM ~]# 


   
Destroy complete! Resources: 3 destroyed.
[root@TERRAFORM ~]# 



again give command as  [root@TERRAFORM ~]# terraform apply --auto-approve

after that connect the created terraform instance and cross check the file has came or not











